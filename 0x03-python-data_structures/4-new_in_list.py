#!/usr/bin/python3
def new_in_list(my_list, idx, element):
    i = my_list.copy()
    elo = len(i)
    boss = i.insert(idx, element)
    if idx< 0 or idx > eli:
        return i
    else:
        return boss
